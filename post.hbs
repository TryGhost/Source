{{!< default}}
{{!-- The tag above means: insert everything in this file into the body of the default.hbs template --}}

{{#post}}

<main class="post-main">

    {{!-- Hero Section --}}
    <section class="post-hero" {{#if feature_image}}style="background-image: url('{{img_url feature_image size="xl"}}')"{{/if}}>
        <div class="post-hero-content">
            {{#if tags}}
            <div class="post-hero-tags">
                {{#foreach tags limit="5"}}
                    <a class="post-hero-tag" href="{{url}}">#{{name}}</a>
                {{/foreach}}
            </div>
            {{/if}}

            <div class="post-hero-title">
                <h1>{{title}}</h1>
            </div>

            <time class="post-hero-date" datetime="{{date format="YYYY-MM-DD"}}">
                {{date format="YYYY. MM. DD"}}
            </time>
        </div>
    {{!-- Magazine Label Overlay --}}
        <div class="post-magazine-label">
            {{#if primary_author.profile_image}}
                <img src="{{img_url primary_author.profile_image size="xs"}}" alt="{{primary_author.name}}" width="117" height="46">
            {{/if}}
        </div>
    </section>

    {{!-- Content Section --}}
    <section class="post-content-wrapper">
        <div class="post-content">
            <div class="post-content-inner">

                {{!-- Breadcrumbs and Favorite --}}
                <div class="post-meta-header">
                    <div class="post-breadcrumbs">
                        <a href="{{@site.url}}">Top</a>
                        <span class="post-breadcrumb-separator">›</span>
                        <span>記事詳細</span>
                    </div>
                    {{> "components/post-like" stroke_color="#000" post_id=id}}
                </div>

                {{!-- Rich Text Content --}}
                <div class="post-rich-text">
                    {{content}}
                </div>

                <div class="post-logo">
                    {{#if primary_author.profile_image}}
                        <img src="{{img_url primary_author.profile_image size="xs"}}" alt="{{primary_author.name}}" class="post-author-image">
                    {{/if}}
                </div>
            </div>
        </div>
    </section>
</main>

{{/post}}

{{#get "posts" include="authors" filter="id:-{{post.id}}" limit="3" as |next|}}
    {{#if next}}
        {{> "components/featured-articles" posts=posts}}
    {{/if}}
{{/get}}