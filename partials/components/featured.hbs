{{#if showFeatured}}
    {{#get "posts" filter="featured:true" include="authors" limit=limit as |featured|}}
        {{#foreach featured}}
            {{> "post-card" imageSizes="80px"}}
        {{/foreach}}
    {{/get}}
{{/if}}